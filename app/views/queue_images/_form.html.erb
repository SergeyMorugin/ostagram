<%= form_for(@queue_image, :html => {:multipart => true}) do |f| %>
  <% if @queue_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@queue_image.errors.count, "error") %> prohibited this queue_image from being saved:</h2>

      <ul>
      <% @queue_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Обрабатываемое изображение" %><br>
    <%= f.file_field :content_image %>
  </div>
  <div class="field">
    <%= f.label "Шаблон"%><br>
    <%= f.file_field :style_image %>
  </div>
  <div class="actions">
    <%= f.submit "Добавить в обработку", class: "btn btn-large btn-primary" %>
  </div>
<% end %>
